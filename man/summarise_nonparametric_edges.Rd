\name{summarise_nonparametric_edges}
\alias{summarise_nonparametric_edges}
\title{Summarise Nonparametric Bootstrap Results for Network Edges}
\description{
The \code{summarise_nonparametric_edges} function summarizes nonparametric bootstrap estimates for network edges (or other statistics) generated by \pkg{bootnet}.
It provides bootstrap means, confidence intervals, sample estimates, and whether confidence intervals cover zero, allowing for straightforward evaluation of edge stability and significance.
}
\usage{
summarise_nonparametric_edges(
  nonParametricBoot,
  stat_type = "edge",
  probs = c(0.025, 0.975),
  quiet = TRUE
)
}
\arguments{
\item{\code{nonParametricBoot}}{A list object generated by nonparametric bootstrap procedures (e.g., from \pkg{bootnet}), containing elements \code{bootTable} and \code{sampleTable}.}
\item{\code{stat_type}}{Character. Type of statistic to summarise (e.g., \code{"edge"}, \code{"strength"}). Default is \code{"edge"}.}
\item{\code{probs}}{Numeric vector of length 2 giving the quantiles for confidence intervals of the bootstrap distribution. Default is \code{c(0.025, 0.975)}.}
\item{\code{quiet}}{Logical. If \code{FALSE}, prints the first rows of the summary table for inspection. Default is \code{TRUE}.}
}
\details{
\enumerate{
\item Validates the structure of the input object, ensuring required columns exist in both \code{bootTable} and \code{sampleTable}.
\item Extracts the observed sample values (point estimates) for the specified statistic type.
\item Computes bootstrap summaries by edge (or statistic ID), including mean and confidence interval bounds.
\item Combines observed and bootstrap summaries into a single table.
\item Adds a logical column (\code{ci_covers_zero}) indicating whether the bootstrap confidence interval includes zero, useful for edge significance assessment.
\item Sorts results by the absolute magnitude of the bootstrap mean to highlight strongest effects.
}
}
\value{
A \code{data.frame} (tibble) with the following columns:
\itemize{
  \item{\code{id}}{Edge or statistic identifier.}
  \item{\code{sample}}{Observed (sample) value of the statistic.}
  \item{\code{boot_mean}}{Mean bootstrap estimate across replications.}
  \item{\code{ci_lo}}{Lower bound of bootstrap confidence interval.}
  \item{\code{ci_hi}}{Upper bound of bootstrap confidence interval.}
  \item{\code{n_boot}}{Number of bootstrap replications used.}
  \item{\code{ci_covers_zero}}{Logical indicator of whether the CI includes zero (\code{TRUE} = not significant).}
}
}
\examples{
\dontrun{
# Example with nonparametric bootstrap object
res <- bootnet::bootnet(myNetwork, nBoots = 1000, type = "nonparametric")

edges_summary <- summarise_nonparametric_edges(
  nonParametricBoot = res,
  stat_type = "edge",
  probs = c(0.05, 0.95),
  quiet = FALSE
)

head(edges_summary)
}
}
\author{
Dr. José Ventura-León
}
